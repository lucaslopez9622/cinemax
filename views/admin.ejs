<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <script src="/scripts.js"></script>
  <title>CineMax</title>
  <link rel="stylesheet" href="/styles.css">
 
  <title>Administrador</title>
</head>
<body>

  <header>
    <nav class="nav">
      <h1>CINEMAX</h1>
      <a href="/" class="icon"><i class="fa-solid fa-house"></i></a>
      <button class="btn-toggle-form" onclick="toggleAgregarPelicula()">Agregar Película</button>
    </nav>
  </header>

    
     <div class="custom-carousel">
      <div class="custom-carousel-inner" id="carousel-container">
          <% peliculas.forEach(function(pelicula, index) { %>
              <div class="custom-carousel-item">
                  <img src="/images/<%= pelicula.imagen %>" alt="<%= pelicula.nombre %>">
                  <h3><%= pelicula.nombre %></h3>
                  
                  <div class="btn-group">
                    <button class="btn-edit" onclick="editPelicula('<%= pelicula.id %>', '<%= pelicula.nombre %>', '<%= pelicula.descripcion %>', '<%= pelicula.imagen %>')"> Editar </button>
                    <form action="/deletePelicula" method="POST" onsubmit="return confirmDeletion()">
                        <input type="hidden" name="id" value="<%= pelicula.id %>">
                        <button type="submit" class="btn-eliminar">Eliminar</button>
                        </form>
                  </div>
              </div>
          <% }) %>
      </div>
      <div class="custom-carousel-controls">
          <button onclick="moveCarousel(-1)">&#9665;</button>
          <button onclick="moveCarousel(1)">&#9655;</button>
      </div>
  </div>
  
  
  <div id="edit-form">
      
      <form action="/editPelicula" method="POST">
          <input type="hidden" id="edit-id" name="id">
          <label for="edit-nombre">Nombre</label>
          <input type="text" id="edit-nombre" name="nombre" required>
          <label for="edit-descripcion">Descripción</label>
          <textarea id="edit-descripcion" name="descripcion" required></textarea>
          
          <input type="text" id="edit-imagen" name="imagen" required>
          <button class="btn-save" type="submit">Guardar cambios</button>
      </form>
  </div>

  <div id="form-agregar-container" class="form-agregar-container">
  <h4>AGREGAR PELICULA</h4>
  <form class="form-agregar-pelicula" action="/addPelicula" method="POST">
    <label for="nombre">Nombre:</label>
    <input type="text" id="nombre" name="nombre" class="input-agregar" required>

    <label for="descripcion">Descripción:</label>
    <textarea id="descripcion" name="descripcion" class="textarea-agregar" required></textarea>

    <label for="imagen" class="custom-file-label">Seleccionar Imagen</label>
    <input type="file" id="imagen" name="imagen" class="file-upload" accept="image/*" required>
    
   
    <span id="file-name">Ningún archivo seleccionado</span>

    <button type="submit" class="btn-agregar-pelicula">Agregar</button>
</form>

</div> 

<footer class="footer">
  <p>© 2025 Todos los derechos reservados CINEMAX</p>
</footer>

</body>
</html>